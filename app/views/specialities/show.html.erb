<%= render @speciality %>

<div>
  <%= link_to "Editar esta especialidad", edit_speciality_path(@speciality), class:"btn btn-primary m-2" %>
  <button type="button" class="btn btn-danger m-2" id="btn-del" data-bs-toggle="modal" data-bs-target="#exampleModal" >
      Eliminar esta especialidad
  </button>
</div>
<%= link_to "Volver a especialidades", specialities_path, class:"m-2 btn btn-secondary" %>


<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="exampleModalLabel">Eliminar especialidad</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        ¿Esta seguro de eliminar esta especialidad?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
        <%= button_to "Eliminar", @speciality, method: :delete, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>
</div>
